import { addTaskDiv } from "./allSidebarContent";

// have to make separate modules/files for creating new todos, setting todos as complete, changing todo priority and DOM stuff
addTaskDiv.addEventListener("click", () => {
    console.log("Add Task Clicked.");
});
